/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.gitusercontent.com",
      "images.unsplash.com",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
